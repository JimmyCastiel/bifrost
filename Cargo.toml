[package]
name = "bifrost"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.12"
tokio = { version = "1.46.1", features = ["net", "macros", "rt-multi-thread", "signal", "time", "tracing"] }
tokio-util = { version = "0.7.15", features = ["rt"] }
